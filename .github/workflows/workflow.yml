
- name: Codecov
  on: [push]
  steps:
  - uses: actions/checkout@0.2.1
#  - uses: actions/checkout@master
  - uses: codecov/codecov-action@v1
    with:
      files: ./coverage.xml # optional
      name: codecov-umbrella # optional'
